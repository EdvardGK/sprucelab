/**
 * EMERGENCY VIEWER PAGE
 *
 * Standalone page for testing the emergency viewer.
 * No routing needed - can be accessed directly.
 */

import { EmergencyThatOpenViewer } from '@/components/features/viewer/EmergencyThatOpenViewer';

export default function EmergencyViewerPage() {
  return <EmergencyThatOpenViewer />;
}
