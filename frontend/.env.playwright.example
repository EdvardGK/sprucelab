# Copy to .env.playwright.local (gitignored) and fill in your Sprucelab credentials.
# auth.setup.ts reads these to capture a Playwright auth session once per machine.

PLAYWRIGHT_EMAIL=
PLAYWRIGHT_PASSWORD=

# Optional — override the dev server URL if it's not localhost:5173
# PLAYWRIGHT_BASE_URL=http://localhost:5173
