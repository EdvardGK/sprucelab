# Test IFC Files

Place IFC files here for local development testing.

Files in this directory are gitignored (except this README).

## Usage

1. Drop any `.ifc` file here
2. Run `./dev.sh --seed` to create test project/model records
3. Upload via the frontend or reference files directly in seed data
