# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Settings and validation
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Database (shared with Django - Supabase PostgreSQL)
asyncpg>=0.29.0
sqlalchemy>=2.0.25

# IFC Processing
ifcopenshell>=0.8.0
ifctester>=0.8.0
numpy>=1.26.0

# Drawing extraction (Phase 5)
ezdxf>=1.3.0          # DWG/DXF parsing
pymupdf>=1.24.0       # PDF parsing (text + drawings)

# Document extraction (Phase 6)
python-docx>=1.0.0    # DOCX -> markdown
python-pptx>=1.0.0    # PPTX -> markdown per slide

# Geometry (for spatial stitching - point-in-polygon)
shapely>=2.0.0

# CRS lookup (coordinate reference systems)
pyproj>=3.6.0

# Excel support
pandas>=2.0.0
openpyxl>=3.1.0

# Caching (for loaded IFC files)
redis>=5.0.0

# HTTP client (for Django communication)
httpx>=0.26.0

# File handling
python-multipart>=0.0.6
aiofiles>=23.2.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
