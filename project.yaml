# project.yaml — sidehustle project metadata

# Identity
name: "Sprucelab"
code: SLAB
type: dev
kind: sidehustle

# Notion (personal workspace only — never Skiplum)
notion:
  project_id: ~
  subproject_id: ~

themes: []
rolle: ~
fase: ~
